
var data = [
  {
    _id: "1",        // unique id (required)
    value: 1,      // used to determine relative size of bubbles (required)
    displayText: "genre",// will use _id if undefined
    colorValue: 1, // used to determine color
    selected: false,  // if true will use selectedColor/selectedTextColor for circle/text
 }
]
export default data